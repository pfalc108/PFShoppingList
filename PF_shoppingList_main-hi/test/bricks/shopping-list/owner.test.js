import PFShoppingList from "PF_shoppingList_main-hi";
import { mount, wait } from "uu5g05-test";

describe(`PFShoppingList.Bricks.ShoppingList.Owner`, () => {
  // it("onLoad", async () => {
  //   let onLoad = jest.fn(async () => ({ content: "Hello, world!" }));
  //   let loadDtoIn = { foo: "bar" };
  //   let wrapper = mount(<PFShoppingList.Bricks.ShoppingList.Owner onLoad={onLoad} loadDtoIn={loadDtoIn} />);
  //   expect(onLoad).toHaveBeenCalledTimes(1);
  //   expect(onLoad).lastCalledWith(loadDtoIn);
  //   await wait();
  //   expect(wrapper.text()).toContain("Hello, world!");
  // });
});
